<table class="table">
  <tbody>
    <% results.each do |place, votes| %>
      <tr>
        <td><%= place %></td>
        <td class="text-right"><%= votes %> votes</td>
      </tr>
    <% end %>
  </tbody>
</table>
